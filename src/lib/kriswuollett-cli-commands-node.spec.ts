import { kriswuollettCliCommandsNode } from './kriswuollett-cli-commands-node';

describe('kriswuollettCliCommandsNode', () => {
  it('should work', () => {
    expect(kriswuollettCliCommandsNode()).toEqual(
      'kriswuollett-cli-commands-node'
    );
  });
});
